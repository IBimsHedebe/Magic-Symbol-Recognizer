<UserControl x:Class="Magic_Symbol_Recognizer.Controls.LibraryControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button x:Name="BtnBack" Grid.Column="0" Grid.Row="0" Content="⬅ Zurück zum Hauptmenü" Margin="10" Padding="10,5"/>
        <TextBlock x:Name="TxtTitle" Grid.Column="1" Grid.Row="0" Text="Wähle eine Rune aus" FontSize="20" Foreground="Gold" FontWeight="Bold" VerticalAlignment="Center" Margin="10,10,10,0"/>

        <ListBox x:Name="ListSpells" Grid.Column="0" Grid.Row="1" Margin="10" Background="#1e1e1e" Foreground="White" FontSize="14"/>

        <Grid Grid.Column="1" Grid.Row="1" Background="Black" Margin="10">
            <Canvas x:Name="PreviewCanvas" Background="#050505" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" SizeChanged="PreviewCanvas_SizeChanged"/>
        </Grid>
    </Grid>
</UserControl>