<UserControl x:Class="Magic_Symbol_Recognizer.Controls.BattleArenaControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignHeight="600" d:DesignWidth="800">

    <Grid Background="#121212">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Oben: Bot HP & Aktion -->
            <RowDefinition Height="*"/>
            <!-- Mitte: Quadratisches Schlachtfeld -->
            <RowDefinition Height="Auto"/>
            <!-- Unten: Spieler HP & Buttons -->
        </Grid.RowDefinitions>

        <!-- ========================================== -->
        <!-- OBEN: GEGNER (BOT) STATUS & HP -->
        <!-- ========================================== -->
        <Grid Grid.Row="0" Margin="15,10,15,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <!-- Zurück-Button -->
                <ColumnDefinition Width="*"/>
                <!-- Bot Status -->
            </Grid.ColumnDefinitions>

            <Button x:Name="BtnBack" Content="Aufgeben" Grid.Column="0" VerticalAlignment="Center" Padding="12,6" Background="#333" Foreground="White" BorderThickness="0"/>

            <Border Grid.Column="1" Background="#1a1a1a" CornerRadius="6" Padding="15,8" Margin="15,0,0,0" BorderBrush="#2d2d2d" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="180"/>
                    </Grid.ColumnDefinitions>

                    <!-- Was der Bot gerade tut / gezaubert hat -->
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="Magie-Bot (Gegner)" Foreground="#ff7675" FontSize="12" FontWeight="Bold" />
                        <TextBlock x:Name="TxtBotStatus" Text="Bot konzentriert sich..." Foreground="White" FontSize="15" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>

                    <!-- Bot Lebensbalken -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <ProgressBar x:Name="BotHPBar" Height="14" Minimum="0" Maximum="100" Value="100" Background="#222" Foreground="Crimson"/>
                        <TextBlock x:Name="TxtBotHP" Text="100 / 100 HP" Foreground="LightGray" FontSize="11" HorizontalAlignment="Right" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- ========================================== -->
        <!-- MITTE: QUADRATISCHES SCHLACHTFELD -->
        <!-- ========================================== -->
        <Grid Grid.Row="1" Background="Black" Margin="15,5,15,5">
            <!-- Container hält das InkCanvas bei Fensteränderungen immer perfekt quadratisch -->
            <Grid x:Name="CanvasContainer" HorizontalAlignment="Center" VerticalAlignment="Center" SizeChanged="CanvasContainer_SizeChanged">
                <InkCanvas x:Name="InkCanvasControl" 
                           Background="#050505" 
                           Width="{Binding Width, ElementName=CanvasContainer}" 
                           Height="{Binding Height, ElementName=CanvasContainer}">
                    <InkCanvas.DefaultDrawingAttributes>
                        <DrawingAttributes Color="Cyan" Width="7" Height="7"/>
                    </InkCanvas.DefaultDrawingAttributes>
                </InkCanvas>
            </Grid>
        </Grid>

        <!-- ========================================== -->
        <!-- UNTEN: SPIELER HP & INTERFACE-STEUTERUNG -->
        <!-- ========================================== -->
        <Grid Grid.Row="2" Margin="15,5,15,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <!-- Deine eigene HP links -->
                <ColumnDefinition Width="*"/>
                <!-- Buttons zentriert -->
            </Grid.ColumnDefinitions>

            <!-- Deine HP Anzeige unten links -->
            <Border Grid.Column="0" Background="#1a1a1a" CornerRadius="6" Padding="12,6" BorderBrush="#2d2d2d" BorderThickness="1" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Du (Spieler)" Foreground="LimeGreen" FontSize="12" FontWeight="Bold" />
                    <ProgressBar x:Name="PlayerHPBar" Height="14" Minimum="0" Maximum="100" Value="100" Background="#222" Foreground="LimeGreen" Margin="0,3,0,0"/>
                    <TextBlock x:Name="TxtPlayerHP" Text="100 / 100 HP" Foreground="LightGray" FontSize="11" HorizontalAlignment="Right" Margin="0,2,0,0"/>
                </StackPanel>
            </Border>

            <!-- Steuerungs-Buttons rechts zentriert -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button x:Name="BtnCast" Content="Zauber wirken" Width="140" Height="42" FontSize="14" FontWeight="Bold" Background="DarkBlue" Foreground="White" BorderThickness="0" Margin="5,0,5,0"/>
                <Button x:Name="BtnClear" Content="Löschen" Width="100" Height="42" FontSize="13" Margin="5,0,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
